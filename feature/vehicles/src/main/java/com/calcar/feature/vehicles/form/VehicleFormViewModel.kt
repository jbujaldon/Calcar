package com.calcar.feature.vehicles.form

import androidx.lifecycle.ViewModel
import com.calcar.common.ui.navigation.Navigator

class VehicleFormViewModel(
    private val navigator: Navigator,
) : ViewModel() {
}